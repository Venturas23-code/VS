import React from "react";
import ReactDOM from "react-dom/client";
import Center from "./container/Center/Center.jsx";

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Center />
  </React.StrictMode>
)